import React from "react";
import { View, Text } from "react-native";

export function FriendProfileScreen({ route }) {
  console.log(route);
  return <View>{/* <Text>{user.username}</Text> */}</View>;
}
